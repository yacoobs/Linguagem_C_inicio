#include <stdio.h>

int main(){
    int numero, contador = 1;
    printf("Informe o número escolhido:\n");
    scanf("%d", &numero);
    for(numero <= contador);
    printf("A multiplicação é de : %d", 1 * contador ++);
    
}